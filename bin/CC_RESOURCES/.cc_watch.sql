
SELECT
    ID,
    STATUS AS ST,
    SUBSTR(REGEXP_REPLACE(LABEL, ":.+", ""), 0, 50) AS TITLE,
    SUBSTR(REGEXP_REPLACE(LABEL, ".+:", ""), 0, 100) AS FILE
FROM
    STDIN
WHERE
    QGROUP LIKE "cc%"
ORDER BY
    ID DESC
